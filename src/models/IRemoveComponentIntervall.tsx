export interface IRemoveComponentIntervall {
    deleteOnLastIntervalRemoved: boolean
    chordName: string | null
    notePosition: number
    length: number
    intervalPosition: number
    notes: string[]
}
